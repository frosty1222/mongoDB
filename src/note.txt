npm install util
sudo docker rmi $(sudo docker images -aq) // remove all image 
sudo docker rm $(sudo docker ps -aq) // remove all container
sudo docker logs + docker_container_id // to see docker container logs 
git remote set-url origin https://github.com/frosty1222/mongoDB.git // set new remote url